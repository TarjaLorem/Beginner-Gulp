const num = 40;
const cb = () => {
  Math.floor(40);
};
if (num === true) {
  setTimeout(cb, 2000);
}
//New concat file 
const num = 20;
const cb = () => {
  Math.floor(20);
};
if (num === true) {
  setTimeout(cb, 1000);
}
